// ─── DEPRECATED: 교육지원 도메인 관리 ─────────────────────────────────────────
// 2026-03-30: 이 모듈은 더 이상 사용되지 않습니다.
// 예산 계정 구조가 교육지원 도메인 종속 → 가상교육조직 템플릿 종속으로 변경됩니다.
// 기존 참조 코드에서 호출 오류가 발생하지 않도록 stub 함수만 유지합니다.

function renderIsolationGroups()    { console.info('[DEPRECATED] renderIsolationGroups: 교육지원 도메인 메뉴가 제거되었습니다.'); }
function renderMyIsolationGroups()  { renderIsolationGroups(); }
